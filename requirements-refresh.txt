# Slim dependency set for the offline REFRESH job (crawl → pipeline → warehouse
# → publish). Deliberately excludes the heavy/unused ML stack (torch,
# transformers, bertopic, jupyter) so CI installs in seconds, not minutes.
pyyaml>=6.0
python-dotenv>=1.0.0
playwright>=1.40.0
beautifulsoup4>=4.12.0
fake-useragent>=1.4.0
requests>=2.31.0
polars>=0.20.0
pandas>=2.1.0
pyarrow>=14.0.0
duckdb>=0.9.0
pydantic>=2.0.0
numpy>=1.26.0
scipy>=1.11.0
scikit-learn>=1.3.0
Pillow>=10.0.0
boto3>=1.34.0
